---
import { useTranslations } from '../../i18n/utils';

const lang = Astro.currentLocale as 'es' | 'en' || 'es';
const t = useTranslations(lang);
const l = (path: string) => lang === 'en' ? `/en${path}` : path;
---